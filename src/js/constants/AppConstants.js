var keyMirror = require('react/lib/keyMirror');

module.exports = keyMirror({
    GET_TRACKS_SIMILAR: null,   // Get similar tracks
    UPDATE_INPUT: null,         // Update user input
    UPDATE_HAS_RESULTS: null    // Update user input
});
